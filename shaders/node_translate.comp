#version 450

layout(local_size_x = 64, local_size_y = 1, local_size_z = 1) in;

layout(set = 0, binding = 0) buffer Data {
    vec2 data[];
} buf;

// layout (push_constant) uniform Translation {
//   float dx;
//   float dy;
// }

void main() {
    uint idx = gl_GlobalInvocationID.x;

    float dx = 100.0;
    float dy = 100.0;

    buf.data[idx] += vec2(dx, dy);
}
